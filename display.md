# 📱 블러프볼(BluffBall) 화면 흐름 및 API 연동 기획서

본 문서는 사용자의 게임 진입부터 인게임 직전(카드 세팅)까지의 UI/UX 흐름과 각 화면에서 호출되는 백엔드 API 간의 연동 구조를 정의합니다.

---

### 🚪 1. 로그인 화면 (OAuth Login)
* **화면 UI 구성:**
    * 블러프볼 메인 로고 및 인트로 연출
    * 구글, 카카오 소셜 로그인 버튼 레이아웃
* **프론트엔드 액션:** 유저가 소셜 버튼 클릭 시 해당 플랫폼 OAuth 인증 진행 후 JWT 토큰 수신 및 저장
* **🔌 대응 API:** `POST /api/v1/auth/login/{provider}`

### 🏠 2. 메인 로비 화면 (Main Lobby)
* **화면 UI 구성:**
    * 유저 기본 정보 프로필 (닉네임, 전적, 에너지 잔여량 등)
    * 핵심 진입로 버튼 3개: **[싱글 모드]**, **[커스텀 모드]**, **[클랜 로비]**
* **프론트엔드 액션:** 화면 진입 시 유저의 최신 상태를 동기화하기 위해 프로필 조회 API 자동 호출
* **🔌 대응 API:** `GET /api/v1/users/me`

### 🛡️ 3. 클랜 로비 화면 (Clan Room)
* **화면 UI 구성:**
    * 클랜 정보 및 소속 클랜원 목록 표출
    * 클랜전 통합 전적 및 세부 전적(미니 모드 승/패, 정규 모드 승/패) 시각화
    * 하단 매칭 진입 버튼 2개: **[클랜전 미니(4v4)]**, **[클랜전 정규(10v10)]**
    * **파티(초대) 시스템:** 하단에 클랜원들을 초대하여 파티를 생성할 수 있는 슬롯 배치. 파티가 채워진 상태에서 모드 선택 가능
* **🔌 대응 API:**
    * `GET /api/v1/clans/{clanId}/summary` (클랜 및 모드별 전적 조회)
    * `POST /api/v1/clans/party/create` (클랜전 파티 생성 및 멤버 초대)

### ⚙️ 4. 커스텀 매치 설정 화면 (Custom Room Setup)
* **화면 UI 구성:** 방장이 룰을 커스터마이징할 수 있는 토글 및 셀렉트 박스
    * 이닝 수 선택 (1/3/9 이닝 등)
    * 투수 구종 카드 선택 장수 설정 슬롯
    * 최대 참여 인원 설정 및 방 비밀번호 입력 칸
* **프론트엔드 액션:** 설정 완료 후 '방 만들기' 클릭 시 옵션 데이터를 바디에 담아 요청
* **🔌 대응 API:** `POST /api/v1/match/custom/rooms`

### 👥 5. 커스텀 모드 대기방 화면 (Custom Waiting Room)
* **화면 UI 구성:**
    * **2열 종대 슬롯:** 설정된 인원 수에 맞게 칸이 정렬됨
    * 유저가 있는 칸: 유저 닉네임, 프로필 정보 노출
    * 빈 칸: `+` 마크 버튼 노출 (클릭 시 친구/클랜원 초대 팝업)
    * 하단 버튼: **[게임 시작 (Start)]**, 그 옆 톱니바퀴 모양의 **[설정 변경]** 버튼
* **프론트엔드 액션:** 설정 변경 시 4번 화면의 모달/팝업이 뜨며, 수정 완료 시 방 정보 업데이트 API 호출
* **🔌 대응 API:**
    * `POST /api/v1/match/custom/rooms/{roomId}/settings` (설정 변경)
    * `POST /api/v1/match/custom/rooms/{roomId}/start` (게임 시작 트리거)

### ⏳ 6. 매칭 대기 화면 (Matchmaking Queue)
* **화면 UI 구성:**
    * 중앙: 내 캐릭터 배치 (클랜전/팀전의 경우 내 캐릭터 후방에 팀원들의 캐릭터들이 든든하게 호위하듯 자동 배치되는 연출)
    * 캐릭터와 하단 버튼 사이: 현재 대기 인원 현황 표출 (예: `n / m` 명 ➡️ 매칭 완료 시 `m / m`으로 차오름)
    * 하단: **[매칭 취소]** 버튼
* **프론트엔드 액션:** 매칭이 성사되어 `m/m`이 되는 순간, 클라이언트는 발급받은 `matchSessionId`를 들고 자동으로 다음 7번 화면으로 강제 전환시킴
* **🔌 대응 API:**
    * `POST /api/v1/match/queue/join` (큐 진입)
    * `DELETE /api/v1/match/queue/cancel` (큐 취소)

### 🔢 7. 블러핑 고유 숫자 선택 화면 (Setup Numbers)
* **화면 UI 구성:** 1부터 12까지의 숫자 판넬 오브젝트
    * 싱글 모드: 한 화면에서 내가 투수/타자 역할을 모두 하므로 아웃/병살 및 홈런/3루타 지정을 동시에 진행
    * 팀전(클랜전/커스텀): 내가 맡은 역할(투수 또는 타자)에 맞는 숫자 지정 가이드만 활성화되어 표시
    * **예외 연출:** 경기 도중 투수가 교체되는 시점이 오면, 새로 등판하는 투수 화면에만 이 숫자 선택 화면이 즉시 팝업으로 표출됨
* **프론트엔드 액션:** 제한 시간 종료 혹은 선택 완료 시 데이터를 묶어 서버로 자동 제출
* **🔌 대응 API:** `POST /api/v1/game/{matchSessionId}/setup-numbers`

### 🃏 8. 투수 카드 드로우 연출 화면 (Card Draw Animation)
* **화면 UI 구성:** 투수 역할 유저 전용 화면
    * 화면 중앙에 뒤집힌 카드 더미가 나타나고, 화려하게 셔플(섞기)되는 애니메이션 구동
    * 셔플 후 더미 위에서부터 카드 5장이 한 장씩 부드럽게 뽑혀서 플레이어 앞면으로 뒤집히는 드로우 연출
* **프론트엔드 액션:** 연출이 끝남과 동시에 뽑힌 5장의 카드 데이터가 화면 하단에 정렬됨 (이 단계는 7번 완료 후 서버가 준 카드 리스트 데이터를 클라이언트가 '연출'로 풀어내는 단계)

### 🔄 9. 투수 카드 교체 화면 (Card Mulligan)
* **화면 UI 구성:** 뽑힌 5장의 카드 리스트
    * 마음에 안 드는 카드를 터치하면 '선택' 표시가 되고, 한 번 더 터치하면 '선택 취소' (최대 5장까지 동시 선택 가능)
    * 하단 버튼: **[변경 (1회 제한)]**, 그 옆에 변경 없이 바로 진행하는 **[시작]** 버튼
* **프론트엔드 액션:** [변경] 클릭 시 선택한 카드들을 버리고 새 카드를 받아와 화면을 최종 인게임으로 전환. [시작] 클릭 시 교체 없이 바로 인게임 진입
* **🔌 대응 API:** `POST /api/v1/game/{matchSessionId}/cards/mulligan`

### ⚔️ 10. 투수 투구 선택 화면 (Pitcher Turn)
* **화면 UI 구성:** * **1단계 (구종 선택):** 멀리건 후 살아남은 구종 카드(최대 5장)가 화면 하단에 활성화됨. 투수가 먼저 원하는 구종을 터치하여 선택.
    * **2단계 (좌표 선택):** 구종을 고르면 화면 중앙에 시작 좌표 카드들이 **3~5줄 격자 형태(Grid)**로 쭉 나열되며 활성화됨. 투수가 투구의 '시작 좌표'를 터치하여 최종 선택.
* **프론트엔드 액션:** 구종과 좌표 선택이 완료되면 백엔드로 투구 데이터 전송.
* **🔌 대응 API:** `POST /api/v1/game/{matchSessionId}/pitch`

### ⏳ 11. 타자 대기 화면 (Batter Waiting)
* **화면 UI 구성:** * 투수가 카드 선택을 끝마칠 때까지 타자 화면에 표시되는 간단한 대기 UI.
    * "투수가 구질을 고민 중입니다..." 등의 문구와 함께 대기 애니메이션 표출.
* **프론트엔드 액션:** 투수의 투구 API 호출 응답(또는 소켓 신호)이 올 때까지 대기하다가, 신호가 오면 즉시 12번 화면으로 전환.

### 🎯 12. 타자 예측 및 타격 세팅 화면 (Batter Turn)
* **화면 UI 구성:** 투수가 선택을 마치는 순간 타자 화면이 활성화되며 **5초 모래시계 카운트다운** 시작.
    * **투수 힌트 노출:** 화면 한쪽에 투수가 선택한 '시작 좌표 카드'가 시각적으로 표출됨.
    * **좌표 선택 구역:** 투수의 좌표 카드 레이아웃과 동일하게 격자형 좌표 카드가 나열됨.
    * **💡 좌표 '0' 독립 슬롯:** 격자형 좌표 카드 바로 옆이나 하단에 **[좌표 0 (웨이팅/스윙 안 함)] 카드가 별도의 독립된 버튼**으로 명확하게 표출됨.
    * **타이밍 슬롯:** 화면 중앙에는 5개의 타이밍 칸(`TOO_EARLY` / `EARLY` / `NORMAL` / `LATE` / `TOO_LATE`)이 빈 슬롯 형태로 대기 중. *(타이밍은 카드가 아닌 칸(슬롯) 개념으로, 서버에서 `Timing` enum으로 관리됨)*
* **상호작용 연출 (드래그 앤 드롭 또는 연속 터치):**
    1. 타자가 격자 좌표(또는 0번 카드)를 터치하여 선택.
    2. 이어서 5개 타이밍 칸 중 하나를 선택하면, **선택한 좌표 카드가 해당 타이밍 칸 위로 스르륵 얹어지는(오버레이) 시각적 연출** 발생.
* **⏳ 5초 타임아웃 예외 처리:** * 화면의 5초 카운트다운이 0이 될 때까지 타자가 좌표와 타이밍을 다 올리지 못하면, 클라이언트가 자동으로 `isTimeout=true` 혹은 스윙 미발동 상태로 백엔드에 요청을 날려 턴을 강제 진행시킴.
* **🔌 대응 WebSocket:** `/app/game/{matchSessionId}/batter/select-card`

### 🎬 13. 인게임 결과 연출 및 턴 전환 화면 (Turn Result Animation)
* **화면 UI 구성:** * 타자가 선택을 완료하거나 5초 타임아웃이 되면 즉시 배틀 결과 계산 응답을 수신.
    * **결과 애니메이션:** 주사위가 부여된 경우 화면에 주사위가 굴러가는 연출이 나오고, 최종 판정(안타, 홈런, 헛스윙, 폭투 등)에 맞는 야구 인게임 애니메이션 연출이 2~3초간 진행됨.
    * 연출 종료 후 스트라이크/볼/아웃 전광판 카운트가 업데이트되며 자동으로 다음 턴(10번 또는 공수교대)으로 리셋 전환.